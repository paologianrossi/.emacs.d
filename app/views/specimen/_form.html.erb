<%= form_for(@speciman) do |f| %>
  <% if @speciman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@speciman.errors.count, "error") %> prohibited this speciman from being saved:</h2>

      <ul>
      <% @speciman.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
